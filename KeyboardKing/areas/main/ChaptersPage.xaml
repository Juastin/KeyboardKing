<local:JumpPage x:Class="KeyboardKing.areas.main.ChaptersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KeyboardKing.core"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChaptersPage">
    <Grid>
        <TextBlock Text="Geen episodes beschikbaar" TextWrapping="Wrap" Foreground="White" Margin="55,121,49,49" FontWeight="Bold" FontSize="15"/>
        <Label Content="ChaptersPage" HorizontalAlignment="Left" Margin="49,51,0,0" VerticalAlignment="Top" Foreground="White" FontSize="36"/>
        <ListView Name="EpOverview" Margin="49,114,49,49" HorizontalContentAlignment="Stretch" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="White" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="0" Margin="3 5 3 5" CornerRadius="5" Padding="2" Background="White">
                        <StackPanel >
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="50"/>
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="Episode " FontWeight="Bold" FontSize="18"/>
                                    <TextBlock Text="{Binding [1]}" HorizontalAlignment="Left" FontWeight="Bold" FontSize="18"/>
                                    <TextBlock Text=" "/>
                                    <TextBlock Text="{Binding [2]}" HorizontalAlignment="Left" FontSize="15" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>

            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <Expander IsExpanded="False" Background="LightGray">
                                            <Expander.Header>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="22"/>
                                                    <TextBlock Text=" - " FontSize="22"/>
                                                    <TextBlock Text="{Binding ItemCount}" FontSize="22" FontStyle="Italic"/>
                                                    <TextBlock Text=" episode(s) " FontSize="22" FontStyle="Italic"/>
                                                </StackPanel>
                                            </Expander.Header>
                                            <ItemsPresenter />
                                        </Expander>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
        <Button Content="Refresh" Tag="ChaptersPage" Click="Refresh_Click" HorizontalAlignment="Left" Margin="640,72,0,0" VerticalAlignment="Top" Height="37" Width="111" Background="#FF363636" BorderBrush="White" Foreground="White" FontSize="20"/>
    </Grid>
</local:JumpPage>
